name: Demo Workflow
on: workflow_dispatch
jobs:
 print:
  runs-on: ubuntu-latest
  steps:
    - name: print Hello!
      run: echo Hello workflow!!!
    - name: print few lines
      run: | 
       echo first line
       echo second line
    - name: print few lines
      run: | 
       echo hello!
       echo Iam learning ci/cd

